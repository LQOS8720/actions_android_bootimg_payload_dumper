name: get_images_from_payload.bin

on:
  workflow_dispatch:
    inputs:
      UPLOAD_CONFIG:
        description: 'Upload Artifacts to Actions'
        required: true
        default: "false"
        type: boolean
      ROM_URL:
        description: 'ROM direct link (must contain payload.bin)'
        required: true
        default: "https://ultimateota.d.miui.com/OS2.0.206.0.VLFCNXM/diting-ota_full-OS2.0.206.0.VLFCNXM-user-15.0-ffb9d90a59.zip?t=1764465831&s=6cb61215b740756331758beb4f6a5b3a"
        type: string
      PARTITIONS:
        description: 'Partitions to extract (e.g., boot,vendor_dlkm,vendor_boot)'
        required: true
        default: "boot"
        type: string

env:
  TZ: Asia/Shanghai
  Payload_Dumper_Go_Url: https://github.com/ssut/payload-dumper-go/releases/download/1.2.2/payload-dumper-go_1.2.2_linux_amd64.tar.gz

jobs:
  make:
    runs-on: ubuntu-latest
    steps:
      - name: Clean Up Disk Space
        run: |
          docker images -q | xargs -r docker rmi || echo "No Docker images to remove."

      - name: Create working directory
        run: |
          mkdir -p "$HOME/work"

      - name: Download payload-dumper-go
        run: |
          cd "$HOME/work"
          wget -O payload_dumper_go.tar.gz "$Payload_Dumper_Go_Url"

      - name: Extract payload-dumper-go
        run: |
          cd "$HOME/work"
          # List contents first to inspect structure (for debugging)
          tar -tzf payload_dumper_go.tar.gz
          # Extract and flatten top-level dir if needed
          mkdir -p payload_dumper_go
          tar -xzf payload_dumper_go.tar.gz -C payload_dumper_go --strip-components=1

      - name: Verify and make executable
        run: |
          cd "$HOME/work"
          if [ ! -f payload_dumper_go/payload-dumper-go ]; then
            echo "❌ ERROR: payload-dumper-go not found after extraction!"
            ls -R payload_dumper_go/
            exit 1
          fi
          chmod +x payload_dumper_go/payload-dumper-go
          echo "✅ payload-dumper-go is ready."

      - name: Download ROM
        run: |
          cd "$HOME/work"
          wget -O rom.zip "${{ github.event.inputs.ROM_URL }}"

      - name: Extract ROM to find payload.bin
        run: |
          cd "$HOME/work"
          mkdir -p rom_extracted
          unzip -q rom.zip -d rom_extracted
          # Search for payload.bin anywhere in the extracted ZIP
          find rom_extracted -type f -name "payload.bin" -exec cp {} payload_dumper_go/ \;
          if [ ! -f payload_dumper_go/payload.bin ]; then
            echo "❌ ERROR: payload.bin not found in ROM!"
            find rom_extracted -type f | head -n 20
            exit 1
          fi
          echo "✅ payload.bin found and copied."

      - name: Extract specified partitions
        run: |
          cd "$HOME/work/payload_dumper_go"
          ./payload-dumper-go -o "$HOME/work/payload_dumper_go" -p "${{ github.event.inputs.PARTITIONS }}" payload.bin

      - name: List extracted images
        run: |
          cd "$HOME/work/payload_dumper_go"
          ls -lh *.img || echo "⚠️ No .img files extracted."

      - name: Upload Artifact to GitHub
        if: ${{ github.event.inputs.UPLOAD_CONFIG == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: Extracted-Images
          path: ${{ runner.home }}/work/payload_dumper_go/*.img

      - name: Upload via WeTransfer
        if: ${{ github.event.inputs.UPLOAD_CONFIG == 'false' }}
        run: |
          cd "$HOME/work"
          # Install file-transfer tool
          curl -sL https://git.io/file-transfer | sh
          # Enable nullglob to handle no-match safely
          shopt -s nullglob
          img_files=(payload_dumper_go/*.img)
          if [ ${#img_files[@]} -eq 0 ]; then
            echo "❌ No .img files to upload!"
            exit 1
          fi
          echo "📤 Uploading ${#img_files[@]} image(s) via WeTransfer..."
          ./transfer wet "${img_files[@]}"
